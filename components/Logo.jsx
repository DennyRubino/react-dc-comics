export default function Logo({ width }) {
  return (
    <div>
      <img
        src="/img/dc-logo.png"
        alt="Logo DC"
        width={width}
        className="img-header"
      />
    </div>
  );
}
